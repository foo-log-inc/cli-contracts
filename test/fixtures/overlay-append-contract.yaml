extends: base-contract.yaml
cli_contracts: 0.1.0
info:
  title: Overlay CLI
  version: 2.0.0
command_sets:
  base-cli:
    global_options:
      $append:
        - name: verbose
          aliases: [v]
          description: Enable verbose output.
    commands:
      status:
        summary: Show status.
        exits:
          "0":
            description: Success.
