extends: base-contract.yaml
cli_contracts: 0.1.0
info:
  title: Overlay CLI
  version: 2.0.0
command_sets:
  base-cli:
    commands:
      hello:
        summary: Say hello (customized).
      greet:
        summary: Greet someone.
        exits:
          "0":
            description: Success.
