/**
 * Stub for agent-contracts-runtime generated DSL registries.
 *
 * When agent-contracts-runtime is installed and `npx agent-runtime generate`
 * is run from the dsl/ directory, this file will be replaced with typed
 * registries (agentRegistry, taskRegistry, handoffSchemas).
 *
 * Until then, the auditor falls back to empty registries, which allows
 * the runtime to still execute tasks using its default behavior.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const agentRegistry: Record<string, any> = {};
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const taskRegistry: Record<string, any> = {};
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const handoffSchemas: Record<string, any> = {};
